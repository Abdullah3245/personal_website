// Deprecated: replaced by ParticleField.tsx (unified scroll-driven particle
// system). Safe to delete this file. Kept as an empty module only because the
// sandbox could not remove it during the revamp.
export {}
